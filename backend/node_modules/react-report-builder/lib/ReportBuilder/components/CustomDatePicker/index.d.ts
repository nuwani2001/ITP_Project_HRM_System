import React from 'react';
import "../../../style/components/customDatePicker.scss";
interface IProps {
    name: string;
    startDate: string;
    minDate?: string;
    maxDate?: string;
    onDateInputChangedCallback(e: React.ChangeEvent): any;
    onDateSelectedCallback(name: string, value: string): any;
}
interface IState {
    previousDate: string;
}
export declare class CustomDatePicker extends React.Component<IProps, IState> {
    constructor(props: any);
    render(): JSX.Element;
    private handleChange;
    private handleBlur;
    private handleSelect;
}
export {};
