import { IDimension } from "../../models/node";
import { IAction, IAsyncState } from "../action";
export declare function dimensions(state: IAsyncState<IDimension[]>, action: IAction): IAsyncState<IDimension[]>;
