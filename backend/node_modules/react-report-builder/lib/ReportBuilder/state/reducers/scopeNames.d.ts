import { IAction, IAsyncState } from "../action";
export interface IScopeNamesState {
    scopeNames: IAsyncState<string[]>;
    selectedScope: string;
}
export declare function scopeNames(state: IScopeNamesState, action: IAction): IScopeNamesState;
